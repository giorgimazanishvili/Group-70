// რიცხვების მასივი
const numbers = [5, 10, 15, 3, 25, 8];

// filter() მეთოდი — მხოლოდ 10-ზე მეტი რიცხვები
const filteredNumbers = numbers.filter(num => num > 10);

// მიღებული მასივის დაბეჭდვა
console.log(filteredNumbers);