class bww {
  constructor(brand, model, year, color, engine) {
    this.brand = brand;       // ბრენდი
    this.model = model;       // მოდელი
    this.year = year;         // გამოშვების წელი
    this.color = color;       // ფერი
    this.engine = engine;     // ძრავის ტიპი
  }
}
