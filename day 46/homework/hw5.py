# 5) ახსენით auto და გააკეთეთ 2 მაგალითი

# auto არის C++11-ში შემოღებული კლავიშური სიტყვა, რომელიც გამოიყენება ცვლადების ტიპის ავტომატურად განსაზღვრისთვის კომპილერის მიერ. როდესაც ცვლადი გამოაცხადება auto-თი, კომპილერი განსაზღვრავს მის ტიპს გამოხატულების საფუძველზე, რომელსაც ის ინახავს.

# მაგალითი 1: ცვლადის ტიპის ავტომატური განსაზღვრა
# auto x = 5;      // x არის int ტიპის
# auto y = 3.14;   // y არის double ტიპის

# მაგალითი 2: ვექტორის ელემენტების ტიპის ავტომატური განსაზღვრა
#include <vector>
#include <iostream>

# int main() {
#     std::vector<int> vec = {1, 2, 3, 4, 5};
#     for (auto it = vec.begin(); it != vec.end(); ++it) {
#         std::cout << *it << " ";
#     }
#     return 0;
# }